{
  "name": "anoopkurup-website",
  "version": "1.0.0",
  "description": "Personal marketing website built with Hugo and TinaCMS",
  "scripts": {
    "dev": "npx tinacms dev -c \"hugo server -D\"",
    "build": "npx tinacms build && hugo --minify",
    "tina:dev": "npx tinacms dev",
    "tina:build": "npx tinacms build"
  },
  "dependencies": {
    "@tinacms/datalayer": "^1.2.30",
    "next-auth": "^4.21.1",
    "tinacms": "^2.2.1",
    "tinacms-authjs": "^1.0.2",
    "tinacms-gitprovider-github": "^1.0.12",
    "upstash-redis-level": "^1.0.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.5.30",
    "@types/node": "^20.0.0"
  }
}
